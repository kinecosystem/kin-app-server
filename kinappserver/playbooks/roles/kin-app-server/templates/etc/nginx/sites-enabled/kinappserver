server {
    listen 80;
    server_name {{ nginx_domain }};

    location / {
    proxy_set_header X-Forwarded-For $remote_addr; # erase any pre-existing x-forwarded-for
	proxy_pass         "http://127.0.0.1:8000";
    }
}
